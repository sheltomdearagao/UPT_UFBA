import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.plataformanova',
  appName: 'Plataforma_nova',
  webDir: 'dist'
};

export default config;
